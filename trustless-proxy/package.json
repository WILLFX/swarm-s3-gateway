{
  "name": "@swarm-s3-gateway/trustless-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Local S3-compatible proxy for TrustlessPrivate buckets",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js",
    "build": "tsc"
  },
  "dependencies": {
    "@aws-crypto/client-node": "^4.2.1",
    "@aws-crypto/material-management-node": "^4.2.1",
    "@aws-sdk/client-s3": "^3.0.0",
    "@smithy/node-http-handler": "^4.0.0",
    "fastify": "^5.0.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
